<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>hello</h1>
    <div><%= post.title %></div>
    <img src="<%= post.image %>">
    <div><%= post.description %></div>
    <div><%= post.dateMade %></div>
    <button><a href="/posts/edit/<%= post._id %>">Edit</a></button>
    <form action='/posts/<%= post._id %>?_method=DELETE' method='POST'>
        <button>Delete</button>
    </form>
</body>
</html>
